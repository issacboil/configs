# installs all the programs required for my configs 

# exits if command fails 
set -e 

# cd into the script file location 
cd "$(dirname "$0")"

echo ""
echo "install i3-gaps rofi thunar ly polybar terminator yay xorg i3-lock"
